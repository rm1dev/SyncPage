CREATE TABLE "SyncTombstone" ("id" TEXT NOT NULL, "entityType" TEXT NOT NULL, "entityKey" TEXT NOT NULL, "deletedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, CONSTRAINT "SyncTombstone_pkey" PRIMARY KEY ("id")); CREATE INDEX "SyncTombstone_entityType_deletedAt_idx" ON "SyncTombstone"("entityType", "deletedAt"); CREATE INDEX "SyncTombstone_entityKey_idx" ON "SyncTombstone"("entityKey");
